#ifndef PARTICLE_H
#define PARTICLE_H

class Particle
{
public:
//    Particle(Vector3f mean, Vector3f deviation/*, Vector3f ref_frame, Vector3f new_frame*/);
//        # Initialization of the particle
//        self.position = []
//        self.velocity = [.0, .0, .0]

//        for i in range(3):
//            # Randomly place the particles according to mean and deviation
//            self.position.append(random.normalvariate(mean[i], deviation[i]))

//        # Calculate the cost (score) of the random particle
//        self.cost = score(self.position, ref_frame, new_frame)

//        # Set the best position and the best cost to the initial ones
//        self.best_position = self.position
//        self.best_cost = self.cost
//        self.distance = 0


//    def euclidian_distance(self):
//        l = 0.085
//        if all([self.position[i] == self.best_position[i] for i in range(3)]):
//            self.distance = l * self.distance

//        sum_v = 0
//        for i in range(3):
//            sum_v += (float(self.position[i] - self.best_position[i])) ** 2

//        self.distance = l * sqrt(sum_v)

};

#endif // PARTICLE_H
